<main>
    <a href="/scrape">SCRAPE</a>
{{!-- post artciles that are saved here --}}
<div id="listOfArticles">
    <h3>Articles</h3>
    <ul id="list-of-articles" class="list-group">
      {{#each articles}}
      <li data-id="{{this.id}}" class="list-group-item">
          <p>HERE IS AN IMAGE</p>
          <img src={{this.img}}>
          <p>THIS IS THE TITLE</p>
        <p>{{this.title}}</p>
<p>THIS IS THE DESCRIPTION</p>
        <p>{{this.description}}</p>
        <p>HERE IS THE LINK</p>
        <a href={{this.link}}>{{this.link}}</a>

        <button id="saveBtn" data-id="{{this.id}}"  class="btn btn-danger">save</button>
      </li>
      {{/each}}
    </ul>
  </div>

    {{!-- have delete button for article --}}

    {{!-- button to view notes (modal) --}}
        {{!-- button to delete note --}}
</main> 